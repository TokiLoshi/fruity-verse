export default function Fruit() {
  // Add fruit models
  // Make them float towards the black hole
  // Add them at random from the args
  // If any of them touch the black hole they should disappear
  // IF there's no fruit left the game should end
  return null
}
